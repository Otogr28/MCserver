# Mods SOLO de cliente: NO deben llegar al servidor (lo crashearían) pero
# SÍ se distribuyen a los jugadores por GitHub dentro de mods/.
#
# mc-update (en el VPS) usa este archivo como --exclude-from al hacer rsync de
# mods/ al server de Crafty, así que estos jars nunca llegan al servidor.
#
# Sintaxis: patrones rsync, uno por línea, relativos a mods/. Usa globs para
# sobrevivir cambios de versión. Las líneas que empiezan con # se ignoran.

translate-*.jar
Forgematica-*.jar
MaFgLib-*.jar
oculus-*.jar
embeddium-*.jar

# Axiom (editor de mundos, jar Fabric) corre sobre Forge vía Sinytra Connector
# + Forgified Fabric API. Desde 2026-06-12 los tres VAN al server (Connector es
# both-side y Axiom necesita su mitad server para poder editar el mundo; el uso
# in-game queda restringido a ops). Si el server no arranca: volver a listarlos
# aquí (Axiom-*.jar / Connector-*.jar / fabric-api-*.jar) y mc-update.
