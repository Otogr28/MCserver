# ===== Claves / secretos / contexto admin (NUNCA subir) =====
mcserver_key
mcserver_key.pub
CLAUDE.md

# ===== Audio con copyright para cinemáticas (NO subir al repo público) =====
# La canción del epílogo (descargada de YouTube). El sounds.json + el script + el README SÍ se versionan;
# solo el .ogg queda local. Para que los jugadores lo escuchen, distribuilo aparte (o quitá esta línea si
# asumís la redistribución pública).
kubejs/assets/summerbuddies/sounds/*.ogg
# secretos sueltos (ej. API key de DeepL para el sidecar de voicetrans)
.secrete
.secret
.secret*
*.secret
# tooling/hooks de la laptop del admin (no es modpack)
.claude/
# ruta local de la instancia de cada jugador (la crea sync.ps1, es personal)
.sync-target.txt
# notas personales en markdown (Obsidian, etc.) — NO son modpack. README.md sí se versiona.
*.md
!README.md
# guía operativa de dimensiones/portales (para futuras sesiones de Claude / admin)
!howtoadddimensions.md
.obsidian/
# fichas de documentación de mods (Obsidian, generadas localmente) — NO son modpack
infomods/
gen_infomods.py
*.pem
*.key
id_ed25519*
id_rsa*

# ===== Mundo (cambia cada tick, NO se versiona) =====
world/
world_nether/
world_the_end/
DIM*/
**/playerdata/
**/stats/
**/advancements/
**/region/
**/data/
**/poi/
**/entities/
session.lock
# OJO: los datapacks del modpack SÍ se versionan; la regla world **/data/ de arriba no debe comérselos.
!datapacks/**

# ===== Logs y crashes =====
logs/
crash-reports/
*.log
*.log.gz
debug/
hs_err_pid*.log
.mixin.out/

# ===== Estado del servidor / cache (por instancia) =====
usercache.json
usernamecache.json
banned-players.json
banned-ips.json
ops.json
whitelist.json
.console_history
*.dat_old
# server.properties: solo del servidor (puede llevar rcon.password/IP). Los jugadores no lo necesitan.
server.properties
# translator.config: lleva API key de DeepL (secreto, personal). Se versiona la plantilla .example.
config/translator.config

# ===== Backups / temporales =====
backups/
*.tmp
*.bak
*.zip
*.tar.gz
# OJO: los shaderpacks SÍ se versionan (son .zip que los jugadores copian a shaderpacks/).
!shaderpacks/
!shaderpacks/*.zip

# ===== Runtime del servidor (NO lo necesitan los jugadores) =====
libraries/
/*.jar
run.sh
run.bat
user_jvm_args.txt
eula.txt
.fabric/
versions/
# OJO: mods/*.jar SÍ se versiona (es el modpack). Solo se ignora el .jar de Forge en la raíz.

# ===== Lo que SÍ se versiona (referencia, no borrar):
#   mods/  config/  defaultconfigs/  kubejs/  scripts/
#   server.properties  *.toml  resourcepacks/  datapacks de modpack
# =====
